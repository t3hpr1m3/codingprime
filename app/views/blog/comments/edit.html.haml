- if params[:preview_button]
  %h2 Edit Comment Preview
- else
  %h2 Edit Comment
- if params[:preview_button]
  = render @comment
= render 'form', comment: @comment
= link_to 'Back', blog_post_comments_path(@post)
