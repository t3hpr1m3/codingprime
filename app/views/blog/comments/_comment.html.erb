        <% div_for comment do %>
          <strong><%= comment.author_site ? link_to( comment.author_name, comment.authorr_site ) : comment.author_name %></strong>
          <%- unless comment.new_record? -%>
          <%=h comment.created_at.to_s( :post_date ) %>
          <%- end -%>
          <%= simple_format( comment.comment_text ) -%>
          <%- unless comment.new_record? -%>
          <%- if admin? -%>
          <%= link_to 'Delete', blog_post_comment_path( @post.id, comment.id ), :confirm => "Are you sure?", :method => :delete %>
          <%- end -%>
          <%- end -%>
        <% end -%>

